# 监督学习阶段保真版 A/B 实时结果

这个文件由 `mortal/run_sl_fidelity.py` 自动维护。

- 它是 run-scoped snapshot，不是当前默认 handoff
- 如果它与 `docs/agent/current-plan.md`、`docs/agent/mainline.md`、`docs/status/supervised-verified-status.md` 或 `docs/status/p1-selection-canonical.md` 冲突，以后者为准
- 文件路径保持稳定，是为了和当前代码里的自动写入逻辑对齐
